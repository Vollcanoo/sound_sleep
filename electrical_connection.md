# 电气连接设计（Electrical Connection）

最终连接总览
器件	引脚	连接对象
ESP32	GPIO7	左驱动模块 High Trigger+
ESP32	GPIO8	右驱动模块 High Trigger+
ESP32	GPIO9	左AO3400A Gate（100Ω）
ESP32	GPIO10	右AO3400A Gate（100Ω）
ESP32	GND	公共GND
ESP32	5V	电池A 5V+
电池A	+	左驱动DC+、左阀+、ESP32
电池B	+	右驱动DC+、右阀+
电池A	-	公共GND
电池B	-	公共GND
左驱动 OUT+	左气泵+	
左驱动 OUT-	左气泵-	
右驱动 OUT+	右气泵+	
右驱动 OUT-	右气泵-	
左AO3400A Drain	左阀负极	
左AO3400A Source	公共GND	
左AO3400A Gate	GPIO9	
右AO3400A Drain	右阀负极	
右AO3400A Source	公共GND	
右AO3400A Gate	GPIO10

## 1. 系统概述

本项目采用 ESP32-S3 作为主控制器，负责读取睡姿识别结果，并控制左右气泵及左右泄气阀，实现气囊高度调节。

执行器采用独立驱动方式：

- 气泵：MOS驱动模块驱动
- 泄气阀：AO3400A MOSFET驱动
- 左右执行器分别由两块5V电池供电

系统总体结构如下：

```
                 ESP32-S3
                     │
        ┌────────────┴────────────┐
        │                         │
 GPIO7~10                   Wi-Fi/BLE

        │
        ▼

 ┌───────────────┬────────────────┐
 │               │                │
 ▼               ▼                ▼

左气泵模块    右气泵模块     AO3400A×2

 │               │                │
 ▼               ▼                ▼

左气泵         右气泵      左右泄气阀
```

---

# 2. 电源方案

系统采用双电池供电。

```
电池A

↓

左气泵

↓

左泄气阀

↓

ESP32
```

```
电池B

↓

右气泵

↓

右泄气阀
```

说明：

- 电池A负责左侧执行器及ESP32供电；
- 电池B负责右侧执行器供电；
- 两块电池正极互不连接；
- 两块电池负极必须与ESP32 GND形成公共参考地。

---

# 3. 公共地（Common GND）

系统所有控制信号必须具有统一参考电位，因此以下节点连接为公共GND：

```
电池A负极
        │
电池B负极
        │
ESP32 GND
        │
左AO3400A Source
        │
右AO3400A Source
        │
左气泵驱动模块 Signal GND
        │
右气泵驱动模块 Signal GND
```

注意：

```
公共GND必须连接。
```

否则GPIO输出无法正确驱动AO3400A和MOS驱动模块。

---

# 4. GPIO分配

建议GPIO如下：

| GPIO | 功能 |
|------|------|
| GPIO7 | 左气泵 |
| GPIO8 | 右气泵 |
| GPIO9 | 左泄气阀 |
| GPIO10 | 右泄气阀 |

说明：

后续若GPIO被其它模块占用，可调整为其它普通数字输出引脚。

---

# 5. 左气泵连接

## 控制侧

```
ESP32 GPIO7

↓

MOS驱动模块 High Trigger +

ESP32 GND

↓

Signal GND
```

Low Trigger 不使用。

---

## 功率侧

```
电池A +

↓

DC+

MOS驱动模块

↓

OUT+

↓

左气泵 +

左气泵 -

↓

OUT-

↓

DC-

↓

电池A -
```

---

## 续流保护

在左气泵两端并联一只 SS34 肖特基二极管：

```
SS34 阴极（带色环）

↓

气泵正极

SS34 阳极

↓

气泵负极
```

作用：

- 吸收电机断电时产生的反向电动势；
- 防止损坏驱动模块；
- 降低电磁干扰。

---

# 6. 右气泵连接

连接方式与左侧完全一致。

控制GPIO：

```
GPIO8
```

供电：

```
电池B
```

同样在气泵两端并联SS34。

---

# 7. 左泄气阀连接

泄气阀采用 AO3400A N沟道MOSFET 驱动。

## AO3400A引脚

| 引脚 | 名称 |
|------|------|
|1|Gate|
|2|Source|
|3|Drain|

---

## 电源连接

```
电池A +

↓

左泄气阀 +

左泄气阀 -

↓

AO3400A Drain

AO3400A Source

↓

公共GND
```

---

## 控制连接

```
ESP32 GPIO9

↓

100Ω电阻

↓

Gate
```

Gate增加：

```
10kΩ

↓

公共GND
```

作为下拉电阻。

作用：

- ESP32上电时默认关闭；
- 防止MOS误导通。

---

## 续流保护

泄气阀属于感性负载。

在阀门两端并联SS34：

```
SS34阴极

↓

阀门正极

SS34阳极

↓

阀门负极
```

作用：

吸收线圈反向感应电压。

---

# 8. 右泄气阀连接

完全相同。

GPIO：

```
GPIO10
```

供电：

```
电池B
```

控制：

AO3400A

续流：

SS34

---

# 9. 电池接线

## 电池A正极

通过电源分线端子连接：

```
电池A+

├── 左气泵DC+
├── 左泄气阀+
└── ESP32 5V
```

建议使用：

```
WAGO 221-415
```

五孔分线端子。

---

## 电池B正极

```
电池B+

├── 右气泵DC+
└── 右泄气阀+
```

建议：

```
WAGO 221-413
```

三孔分线端子。

---

## 公共GND

建议使用：

```
WAGO 221-430
```

十孔分线端子。

连接：

```
电池A-

电池B-

ESP32 GND

左驱动模块DC-

右驱动模块DC-

左驱动模块Signal GND

右驱动模块Signal GND

左AO3400A Source

右AO3400A Source
```

---

# 10. 电源滤波

每块电池增加：

```
1000~2200uF

10V

电解电容
```

并联：

```
0.1uF

陶瓷电容
```

连接：

```
5V

↓

电容

↓

GND
```

作用：

- 降低启动压降；
- 降低噪声；
- 提高系统稳定性。

---

# 11. 导线建议

| 用途 | 建议 |
|------|------|
|电池→驱动模块|20~22AWG|
|驱动模块→气泵|20~22AWG|
|电池→泄气阀|22AWG|
|GPIO信号|杜邦线|
|公共GND主线|20~22AWG|

说明：

不要使用杜邦线作为气泵供电线。

---

# 12. 控制逻辑

系统控制关系如下：

| GPIO | 状态 | 执行器 |
|------|------|---------|
|GPIO7 HIGH|启动|左气泵|
|GPIO7 LOW|停止|左气泵|
|GPIO8 HIGH|启动|右气泵|
|GPIO8 LOW|停止|右气泵|
|GPIO9 HIGH|开启|左泄气阀|
|GPIO9 LOW|关闭|左泄气阀|
|GPIO10 HIGH|开启|右泄气阀|
|GPIO10 LOW|关闭|右泄气阀|

---

# 13. 系统工作约束

允许：

```
左气泵 + 右气泵
```

允许：

```
左泄气阀 + 右泄气阀
```

允许：

```
左充气 + 右泄气
```

允许：

```
右充气 + 左泄气
```

禁止：

```
左气泵 + 左泄气阀
```

禁止：

```
右气泵 + 右泄气阀
```

避免同时充气和放气导致能量浪费及压力无法建立。

---

# 14. 上电测试流程

建议按以下顺序测试：

1. ESP32单独上电；
2. 检查GPIO默认状态均为LOW；
3. 接入MOS驱动模块；
4. 测试驱动模块指示灯；
5. 接入单个气泵；
6. 测试左侧充气；
7. 接入AO3400A；
8. 测试泄气阀；
9. 完成左侧测试；
10. 重复测试右侧；
11. 最后测试左右同时工作。

---

# 15. 注意事项

1. 电池A、B正极不得直接连接。
2. 两块电池负极必须与ESP32形成公共GND。
3. AO3400A Gate必须增加10kΩ下拉电阻。
4. 每个感性负载均应增加续流二极管。
5. 气泵供电不得经过ESP32开发板。
6. 电池输出建议增加滤波电容。
7. 所有电源线应使用20~22AWG多股导线，不建议使用普通杜邦线供电。
8. 上电前应检查AO3400A Source、Drain及续流二极管方向是否正确。

---

# 16. 最终电气结构

```
                      ESP32-S3
        ┌──────────────────────────────────┐
        │ GPIO7 → 左气泵驱动模块           │
        │ GPIO8 → 右气泵驱动模块           │
        │ GPIO9 → 左AO3400A               │
        │ GPIO10→ 右AO3400A               │
        │ 5V ← 电池A                      │
        │ GND ← 公共GND                   │
        └──────────────────────────────────┘

电池A：
    ├── 左气泵驱动模块 → 左气泵
    ├── 左AO3400A → 左泄气阀
    └── ESP32

电池B：
    ├── 右气泵驱动模块 → 右气泵
    └── 右AO3400A → 右泄气阀

公共GND：
    电池A- = 电池B- = ESP32 GND = 两个AO3400A Source = 两个驱动模块Signal GND
```
