# 气路系统设计（Pneumatic System）

## 1. 系统概述

本项目采用左右双气囊结构，通过两个微型气泵分别控制左右气囊充气，并使用两个常闭电磁泄气阀实现独立放气，从而完成枕头高度调节，实现睡姿自动矫正。

整个系统采用左右独立控制方案，每侧均具有独立的充气与泄气能力，可根据姿态识别结果分别调节左右气囊压力。

系统总体结构如下：

```
                 ESP32-S3
                     │
         ┌───────────┴───────────┐
         │                       │
    左侧控制                  右侧控制
         │                       │
    左气泵                   右气泵
         │                       │
     左气囊                 右气囊
         │                       │
    左泄气阀                右泄气阀
```

---

# 2. 系统组成

整个气路系统由以下部分组成。

| 模块 | 数量 | 作用 |
|------|------|------|
| 微型直流气泵（5V） | 2 | 分别为左右气囊充气 |
| 常闭电磁泄气阀（5V） | 2 | 控制左右气囊放气 |
| TPU气囊 | 2 | 调节枕头高度 |
| 硅胶气管（4 mm） | 若干 | 输送空气 |
| 三通接头（4 mm） | 2 | 连接气路 |
| 单向阀（可选） | 2 | 防止回气 |
| ESP32-S3 | 1 | 控制整个系统 |

---

# 3. 气路结构

左右两套气路完全一致。

左侧气路如下：

```
                空气

                 │

             左气泵

                 │

            （单向阀，可选）

                 │

             左气囊

                 │

            左泄气电磁阀

                 │

               大气
```

右侧：

```
                空气

                 │

             右气泵

                 │

            （单向阀，可选）

                 │

             右气囊

                 │

            右泄气电磁阀

                 │

               大气
```

说明：

- 气泵负责向气囊充气；
- 泄气阀负责将气囊中的空气排出；
- 两侧互不影响；
- 每个气囊均可单独控制。

---

# 4. 工作模式

系统共有四种工作状态。

## 4.1 充气

目标：

提高某一侧气囊高度。

控制方式：

```
气泵：开启

泄气阀：关闭
```

空气流向：

```
空气

↓

气泵

↓

气囊
```

---

## 4.2 保持

目标：

保持当前高度。

控制方式：

```
气泵：关闭

泄气阀：关闭
```

此时：

- 气囊保持内部压力；
- 不继续充气；
- 不泄气。

---

## 4.3 泄气

目标：

降低某侧高度。

控制方式：

```
气泵：关闭

泄气阀：开启
```

空气流向：

```
气囊

↓

泄气阀

↓

空气
```

---

## 4.4 左右独立调节

系统允许：

例如：

```
左侧：

充气

右侧：

泄气
```

此时：

```
左气泵：开启

左阀：关闭

右气泵：关闭

右阀：开启
```

从而实现左右高度快速调整。

---

# 5. 控制逻辑

系统遵循以下控制规则。

## 规则一

同一侧：

禁止：

```
气泵+泄气阀 同时开启
```

否则会形成：

```
气泵持续充气

↓

泄气阀持续放气
```

导致：

- 无法建立压力；
- 电池快速耗电；
- 气泵发热；
- 阀门发热；
- 调节失败。

---

## 规则二

允许：

```
左气泵+右气泵
```

同时工作。

---

## 规则三

允许：

```
左泄气阀+右泄气阀
```

同时工作。

---

## 规则四

允许：

```
左充气+右泄气
```

同时工作。

---

## 规则五

允许：

```
右充气+左泄气
```

同时工作。

---

# 6. 电源方案

最终采用：

```
双电池供电
```

电源分配如下：

```
电池A

↓

左气泵

↓

左泄气阀

↓

ESP32
```

```
电池B

↓

右气泵

↓

右泄气阀
```

说明：

两个电池：

```
正极不能直接连接
```

两个电池：

```
负极与ESP32公共GND连接
```

用于提供统一参考电位。

---

# 7. 控制器

系统控制器采用：

```
ESP32-S3
```

负责：

- 睡姿识别结果接收；
- 控制左右气泵；
- 控制左右泄气阀；
- Wi-Fi通信；
- 数据上传。

GPIO分配如下：

| GPIO | 功能 |
|------|------|
| GPIO7 | 左气泵 |
| GPIO8 | 右气泵 |
| GPIO9 | 左泄气阀 |
| GPIO10 | 右泄气阀 |

（如后续软件开发需要，可调整GPIO编号。）

---

# 8. 控制流程

睡姿识别完成后：

```
姿态识别

↓

生成控制指令

↓

ESP32

↓

控制GPIO

↓

驱动模块/AO3400A

↓

气泵/泄气阀

↓

气囊高度变化
```

最终完成睡姿矫正。

---

# 9. 系统特点

本方案具有以下特点：

- 左右气囊完全独立控制；
- 支持左右同时充气；
- 支持左右同时泄气；
- 支持一侧充气、一侧泄气；
- 调节速度快；
- 结构简单；
- 易于扩展压力传感器；
- 易于后续PCB设计；
- 易于软件模块化开发。

---

# 10. 后续优化方向

后续版本可增加以下功能：

1. 增加气压传感器，实现闭环压力控制；
2. 增加PWM控制，实现气泵流量调节；
3. 根据用户睡姿自动调整充放气速度；
4. 增加异常保护（长时间充气自动停止）；
5. 增加低电量检测；
6. 优化气囊结构，提高舒适性。
